diff a/packages/x11/lib/libSM/package.mk b/packages/x11/lib/libSM/package.mk	(rejected hunks)
@@ -17,6 +17,6 @@ PKG_CONFIGURE_OPTS_TARGET="--enable-static \
                            --without-xmlto \
                            --without-fop"
 
-if [ "${PROJECT}" = "L4T" ]; then
+if [ "${PROJECT}" = "L4T" -o "${DEVICE}" = "Odin" ]; then
   PKG_CONFIGURE_OPTS_TARGET="${PKG_CONFIGURE_OPTS_TARGET/--disable-shared/--enable-shared}"
 fi
